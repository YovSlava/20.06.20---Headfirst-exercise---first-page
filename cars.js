var taxi = {
 name: 'Taxi',
 modelNew: false,
 year: 1950,
 color: 'yellow',
 isLicenced: true
};

function isToBeConsidered(taxi) {
 if (taxi.year > 1965) {
  return false
 } else if (isLicenced = false) {
  return false
 }
 return true;
}